<div class="partners-listing">
  <input type="text" class="partners-listing__search text-field">
  <div class="partners-listing__partners">
    {% for partner in collections.partners %}
      <div data-partner-name="{{ partner.title | e | safe }}">{{ partner.title }}</div>
    {% endfor %}
  </div>
</div>